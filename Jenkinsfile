pipeline {
    agent any

    stages {
        stage('Webhook Test') {
            steps {
                echo "Webhook is working"
            }
        }

        stage('Print Files') {
            steps {
                sh 'ls -l'
            }
        }
    }
}
